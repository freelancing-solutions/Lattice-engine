# Shared Environment

Shared configuration used by both CLI implementations.

Sources of truth:
- `.lattice/config.json` at repo root (primary)
- Environment variable `LATTICE_API_URL` (override)

Default API endpoint:
- `https://api.project-lattice.site`